import { EmptyState } from '@/components/shell/empty-state';

export default function DashboardPage() {
  return (
    <EmptyState
      phaseLabel="Phase 8"
      title="Learning Dashboard"
      description="Insights appear after 50+ calls have been logged. Make calls from the Call Console to start building data."
    />
  );
}
