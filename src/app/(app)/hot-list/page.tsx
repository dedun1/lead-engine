import { EmptyState } from '@/components/shell/empty-state';

export default function HotListPage() {
  return (
    <EmptyState
      phaseLabel="Phase 7"
      title="Hot List"
      description="No active triggers yet. Triggers appear after you generate leads and mark a niche as actively pitching."
    />
  );
}
