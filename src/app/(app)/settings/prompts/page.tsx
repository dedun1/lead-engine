import { EmptyState } from '@/components/shell/empty-state';

export default function PromptsPage() {
  return (
    <EmptyState
      phaseLabel="Phase 9"
      title="AI Prompts"
      description="Editable Haiku prompt templates (niche intelligence, openers, summaries, weekly insights). Editor ships in Phase 9 — read-only display in Phase 4."
    />
  );
}
