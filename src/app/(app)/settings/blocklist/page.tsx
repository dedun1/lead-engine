import { EmptyState } from '@/components/shell/empty-state';

export default function BlocklistPage() {
  return (
    <EmptyState
      phaseLabel="Phase 9"
      title="Blocklist Review"
      description="Permanent regeneration prevention. Monthly review keeps the default-forever blocklist correct without abandoning the safety. Ships in Phase 9."
    />
  );
}
