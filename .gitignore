node_modules/
.next/
out/
dist/
.env.local
.env*.local
*.log
.DS_Store
playwright/.cache/
electron-builder.cache/

# Claude Code per-machine permissions — never shared
.claude/settings.local.json

# TypeScript incremental build cache — per-machine, regenerated on every build
tsconfig.tsbuildinfo
